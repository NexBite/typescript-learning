# 3. Install TypeScript
Inside typescript-learning:
npm init -y
Then:
npm install -D typescript
# 4. Create tsconfig.json
 npm install -D tsx
Run:

npx tsc --init


# start project :
 npx tsx main.ts
